//Operator functions
const add = (a, b) => a + b
const subtract = (a, b) => a - b
const multiply = (a, b) => a * b
const divide = (a, b) => a * b

const operate = (operator, num1, num2) => {
   num1 = Number(num1)
   num2 = Number(num2)
};
